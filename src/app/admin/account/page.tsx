import AccountSettings from "@/components/account/AccountSettings";

export default function AdminAccountPage() {
  return (
    <AccountSettings
      title="Account"
      subtitle="Admin profile, notifications, theme, and password — shared with your portal login."
    />
  );
}
